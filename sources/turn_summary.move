module suimon::turn_summary {
    // use sui::object::{Self, UID};
    // use suimon::suimon::Suimon;

    // storable turn summary for a previous turn
    // struct TurnSummary has key, store {
    // }

    // current turn
    // struct Turn {
    // }
}